<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DetailToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="android:actionMenuTextColor">@color/colorAccent</item>
    </style>

    <!-- Sign EditText Style-->
    <style name="SignEditText" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">#bebebe</item> <!--포커스 되어있지 않은 경우의 밑줄 부분-->
        <item name="colorControlActivated">@color/colorAccent</item> <!--손끝으로 터치되어 있는 부분. text selection handle라고 부른다고 한다-->
        <item name="android:textCursorDrawable">@color/colorAccent</item> <!--커서 색. drawable 지정되지만, 색을 설정하는것도 된다-->
        <item name="android:textColor">#000000</item> <!--텍스트 색-->
        <item name="android:textColorHint">#bebebe</item> <!--입력전의 Hint (포커스 Holder)-->
        <item name="android:textColorHighlight">@color/colorAccent</item> <!--텍스트 선택색-->
    </style>

</resources>
